前置条件
Windows 端：

控制面板 → 程序和功能 → 启用或关闭 Windows 功能 → 勾选：
✅ 适用于 Linux 的 Windows 子系统
✅ 虚拟机平台
重启电脑
PowerShell 管理员运行：wsl --install（装最新版）
进 WSL：wsl，设置用户名密码
安装
# 1. 把 setup.sh 拷到 WSL 里（从 Windows 目录）
cp /mnt/c/Users/你的用户名/Downloads/setup.sh ~/
cd ~

# 2. 一键部署
bash setup.sh
# 遇到 pip 提示时按 Y，遇到 bcc 提示时复制 apt 命令执行

# 3. 启动
项目目录: /home/administratorjiangtianen/runtime-guardian
快速开始:
  cd /home/administratorjiangtianen/runtime-guardian
  sudo python3 src/ebpf_monitor.py --no-response

# 4. 另一个 WSL 终端测试
cat /etc/passwd

预期输出
🔴 敏感文件  🟠 调用风暴  🟡 行为链  🔵 网络连接
⚡ load=0.5 mem=180MB 背压=OK GC=342进程 告警=15
# 5. 查看被压缩的日志
sudo cat /sys/kernel/debug/tracing/trace_pipe | grep RG_ALERT
# 6. 发行版在第一runtime-guardin里的runtime-guardin版本号2.02
测试版ai生成经过多重调试才有了现在的发行版。
就一个setup管他什么发行版，测试版用那个setup.sh就对了。

